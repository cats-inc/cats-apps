# Shared Workspace Packages

Reserved for code shared by multiple official apps after a concrete use case exists.
The public App SDK remains owned by cats-platform.

There are no shared packages in this repository yet. Do not create a second
host SDK or import sibling repository source files here.
