# Repository documentation checks require no environment variables.
# Installed utility apps receive their context through cats-platform.
# Provider credentials and runtime endpoint secrets are owned by the host/runtime.
# Add concrete development-only variables with the first app implementation.
